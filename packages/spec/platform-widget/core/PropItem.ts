import { PropItemCompAccessSpec } from '@engine/visual-editor/data-structure';
import React from 'react';

export const PropItem = (propItemMeta: PropItemCompAccessSpec) => {
  return (srouceClass) => {
  };
};
